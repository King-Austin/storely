interface:
  display_name: "Storely"
  short_description: "Repo-specific patterns and workflows for storely"
  default_prompt: "Use the storely repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true